import 'package:flutter/material.dart';

class SplahAnimationWidget extends StatelessWidget {
  const SplahAnimationWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
